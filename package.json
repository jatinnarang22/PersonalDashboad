{
  "name": "personal-dashboard-api",
  "private": true,
  "description": "Express + MongoDB API (backend only — use main branch for full stack)",
  "scripts": {
    "setup": "npm install --prefix server",
    "dev": "npm run dev --prefix server",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "engines": {
    "node": ">=18"
  }
}
