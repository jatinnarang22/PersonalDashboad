{
  "name": "personal-dashboard-client",
  "private": true,
  "description": "Vue 3 dashboard (frontend only — use main branch for full stack)",
  "scripts": {
    "setup": "npm install --prefix client",
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client"
  },
  "engines": {
    "node": ">=18"
  }
}
